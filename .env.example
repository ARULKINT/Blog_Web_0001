# MongoDB connection string (MongoDB Atlas or local)
MONGODB_URI="mongodb://localhost:27017/blog_web"

# Cloudinary (sign up free at cloudinary.com)
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Site
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_SITE_NAME="Ink & Ideas"

# Auth (Phase 2)
NEXTAUTH_URL="http://localhost:3000"
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Newsletter — Resend (resend.com, free tier)
RESEND_API_KEY=""

# Stripe (Phase 3 — stripe.com)
STRIPE_SECRET_KEY=""
STRIPE_PUBLISHABLE_KEY=""
# Create a recurring Price in Stripe dashboard and paste its ID here
STRIPE_PRICE_ID=""
# Webhook secret from `stripe listen` or Stripe dashboard
STRIPE_WEBHOOK_SECRET=""

# Google AdSense (Phase 3 — optional)
NEXT_PUBLIC_ADSENSE_CLIENT=""

# GA4 (Phase 3 — optional)
NEXT_PUBLIC_GA_ID=""

# OpenAI — AI recommendations (Phase 3 — optional)
# Run `npm run ai:embeddings` after seeding to generate embeddings
OPENAI_API_KEY=""
